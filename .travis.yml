---
language: ruby

notifications:
  email:
  - ngiger@ywesee.com

cache: bundler

sudo: false

before_install:
  - gem --version

bundler_args: --without debugger

rvm:
  - ruby-head
  - 2.2.3
  - 2.3.0
matrix:
  allow_failures:
    - rvm: ruby-head
